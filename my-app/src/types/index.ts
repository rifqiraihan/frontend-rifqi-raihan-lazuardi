export interface Negara {
    id: string;
    nama_negara: string;
  }
  
  export interface Pelabuhan {
    id: string;
    nama_pelabuhan: string;
  }
  
  export interface Barang {
    id: string;
    nama_barang: string;
    deskripsi: string;
    harga: number;
    diskon: number;
  }
  